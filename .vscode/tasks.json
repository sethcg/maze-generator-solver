{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cmake --build build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "dependsOn": "Configure"
        },
        {
            "label": "Configure",
            "type": "shell",
            "command": "cmake -S. -Bbuild --preset windows"
        }
    ]
}